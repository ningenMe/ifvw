import type { UnifiedApiModel } from "../core/model.js";

// Placeholder; static site generation lands in issue/011-generate-static-site.md
export async function generateSite(_models: UnifiedApiModel[], _outDir: string): Promise<void> {
  throw new Error("not implemented yet (see issue/011-generate-static-site.md)");
}
