// Placeholder UI entry; real implementation lands in issue/007-implement-service-list-ui.md
document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("app");
  if (root) {
    root.textContent = "ifvw";
  }
});
