openapi: 3.0.3
info:
  title: Sample API
  version: "1.0.0"
paths:
  /users/{id}:
    get:
      operationId: getUser
      summary: Get a user
      responses:
        "200":
          description: OK
  /users:
    post:
      description: Create a user
      deprecated: true
      responses:
        "201":
          description: Created
