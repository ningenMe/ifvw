openapi: 3.0.3
info:
  title: Broken API
  version: "1.0.0"
paths:
  /broken:
    get:
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DoesNotExist"
